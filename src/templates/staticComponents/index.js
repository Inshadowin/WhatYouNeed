import Pop from './Pop';
import Avatar from './Avatar';
import TextArea from './TextArea';
import { Input, Spin } from 'antd';

export default () => {
    return {
        Avatar,
        Pop,
        TextArea,
        Input,
        Spin
    }
}